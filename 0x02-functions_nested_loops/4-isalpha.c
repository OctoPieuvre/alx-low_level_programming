#include main.h
/**
 * main - check for alphabetic
 *
 * Return: 1 if c is letter uppercase or lowercase, 0 otherwise
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
